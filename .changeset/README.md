# Release notes

PatchProof keeps release intent in `CHANGELOG.md` and the release workflow. Future changesets may be added here using the standard Changesets format; publishing remains an explicit maintainer action outside local verification.
